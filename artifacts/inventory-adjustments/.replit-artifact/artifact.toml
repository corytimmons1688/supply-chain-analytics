kind = "web"
previewPath = "/"
title = "Inventory Adjustments Dashboard"
version = "1.0.0"
id = "artifacts/inventory-adjustments"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 5000

[services.development]
run = "pnpm --filter @workspace/inventory-adjustments run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/inventory-adjustments", "run", "build" ]
publicDir = "artifacts/inventory-adjustments/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "5000"
BASE_PATH = "/"
